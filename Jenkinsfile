pipeline {
  agent none
  stages {
    stage('build') {
      steps {
        echo '"build step"'
      }
    }
    stage('test') {
      steps {
        echo 'Testing Stage'
      }
    }
    stage('deploy') {
      steps {
        echo 'deploy stage'
      }
    }
  }
}